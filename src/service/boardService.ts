export default class BoardService {
    static createBoard(board): Promise<string> {
        return Promise.resolve("")
    }

    static getBoards(): Promise<[]> {
        return Promise.resolve([])
    }

    static addStage(stage): Promise<void> {
        return Promise.resolve()
    }
}